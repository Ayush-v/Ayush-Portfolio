import * as React from "react";
import Layout from "../components/layout";

const ProjectPage = () => {
  return (
    <Layout>
      <div>
        <h1>project page</h1>
      </div>
    </Layout>
  );
};

export default ProjectPage;
